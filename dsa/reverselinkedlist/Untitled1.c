// where my ideas might work, or thoughts of might work.

/*while(temp1 != NULL) {
            temp1=temp1->next;
            temp1->next=temp;
            temp1=temp1->next;


    // another one
      temp2=temp1;
            temp2->next=temp;
            temp1=temp2->next;
            temp1=temp1->next;







*/
